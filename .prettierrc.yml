# Prettier configuration
plugins:
  - "prettier-plugin-java"
  - "@prettier/plugin-xml"
overrides:
  - files:
      - "*.java"
    options:
      printWidth: 140
      tabWidth: 2
      useTabs: false
  - files:
      - "*.xml"
    options:
      xmlWhitespaceSensitivity: ignore
      tabWidth: 2
      useTabs: false
